<Page
    x:Class="smartReception.LogOut"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:smartReception"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="#F4F7F9">

    <Grid Background="Transparent">
        <Border Width="420" 
                Height="380" 
                Background="White" 
                CornerRadius="8" 
                HorizontalAlignment="Center" 
                VerticalAlignment="Center"
                BorderBrush="#EAEAEA"
                BorderThickness="1">

            <Border.Shadow>
                <ThemeShadow />
            </Border.Shadow>

            <Grid Padding="30,24,30,24">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,12">
                    <Ellipse Width="60" Height="60" Fill="#FFF0ED"/>
                    <FontIcon Glyph="&#xE7E8;" Foreground="#F04438" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Grid>

                <StackPanel Grid.Row="1" HorizontalAlignment="Center" Margin="0,0,0,18">
                    <TextBlock Text="Logout" 
                               FontSize="24" 
                               FontWeight="Bold" 
                               Foreground="#1D2939" 
                               HorizontalAlignment="Center" 
                               Margin="0,0,0,8"/>
                    <TextBlock Text="Are you sure you want to logout from the system?" 
                               FontSize="13" 
                               Foreground="#475467" 
                               HorizontalAlignment="Center"
                               TextWrapping="Wrap"/>
                </StackPanel>

                <Border Grid.Row="2" 
                        Background="#F8FAFC" 
                        BorderBrush="#E2E8F0" 
                        BorderThickness="1" 
                        CornerRadius="6" 
                        Padding="16,12" 
                        Margin="0,0,0,24"
                        VerticalAlignment="Top">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Grid Grid.Column="0" VerticalAlignment="Center" Margin="0,0,16,0">
                            <Ellipse Width="48" Height="48" Fill="#E0E9F6"/>
                            <FontIcon Glyph="&#xE77B;" Foreground="#3B82F6" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>

                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="Administrator" FontSize="15" FontWeight="SemiBold" Foreground="#1E293B" Margin="0,0,0,4"/>

                            <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                <TextBlock Text="Role:" FontSize="12" Foreground="#64748B" Width="45"/>
                                <TextBlock Text="Administrator" FontSize="12" Foreground="#334155" FontWeight="Medium"/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Email:" FontSize="12" Foreground="#64748B" Width="45"/>
                                <TextBlock Text="admin@building.com" FontSize="12" Foreground="#334155" FontWeight="Medium"/>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </Border>

                <Grid Grid.Row="3">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0" 
                            Content="Cancel" 
                            HorizontalAlignment="Stretch" 
                            Height="40"
                            Background="#D2D6DC" 
                            Foreground="#1F2937" 
                            FontSize="14"
                            FontWeight="SemiBold"
                            CornerRadius="6"
                            x:Name="cancelbtn" Click="cancelbtn_Click">
                        <Button.Resources>
                            <ResourceDictionary>
                                <StaticResource x:Key="ButtonBackgroundPointerOver" ResourceKey="SystemControlPageBackgroundAltHighBrush"/>
                            </ResourceDictionary>
                        </Button.Resources>
                    </Button>

                    <Button Grid.Column="2" 
                            HorizontalAlignment="Stretch" 
                            Height="40"
                            Background="#D92D20" 
                            Foreground="White" 
                            CornerRadius="6"
                            x:Name="logoutbtn" Click="logoutbtn_Click">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <FontIcon Glyph="&#xE7E8;" FontSize="14" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Logout" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </Grid>

            </Grid>
        </Border>
    </Grid>
</Page>